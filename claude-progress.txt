## Claude Session Progress

### 2026-03-07: extract-library-crate (no version bump — structural refactor)

**Changes made:**
- `src/lib.rs` (NEW): Library crate root. Exports `pub mod hal` and `pub mod server`.
  Contains `#[cfg(test)]` embassy-time linker stubs (`_embassy_time_now`,
  `_embassy_time_schedule_wake`) previously in `src/main.rs`.
- `src/main.rs` (DELETED): Binary entry point removed. Crate is now a library.
- `src/storage/` (DELETED): `RamStorageHal` moved to `examples/dev/storage.rs`.
- `src/esp32/` (DELETED): All ESP32 stub implementations moved to `examples/esp32/`.
- `examples/dev/main.rs` (NEW): Host development entry point — spin executor,
  all stub HAL implementations (`StubControlHal` through `StubPasswordHasher`),
  `RamStorageHal`, SSE task stub, `async_main`, `fn main()`.
- `examples/dev/storage.rs` (NEW): `RamStorageHal` (RAM-backed `StorageHal`).
  Imports from `cocktail_bot_hal::hal::`.
- `examples/esp32/main.rs` (NEW): ESP32 reference entry point — spin executor
  placeholder with `// TODO: #[esp_hal::main]` comment; wires sub-structs into
  `ApiServer` + `RobotHal`.
- `examples/esp32/{control,status,config,storage,sensors,dispense,cleaning,hasher}.rs` (NEW):
  All files from former `src/esp32/`, with `use crate::hal::` → `use cocktail_bot_hal::hal::`.
- `Cargo.toml`: Added `[lib]`, `[[example]] name = "dev"`, `[[example]] name = "esp32"
  required-features = ["esp32"]`.
- `CLAUDE.md`: Updated repository structure, build/run commands, AI notes.

**Status:** `cargo test` — 113 tests, 0 failed. `cargo check --features esp32` — clean.
`cargo build --example dev` — clean.

**Next steps:**
- Archive `glass-presence-check` and `implement-test-hal` openspec changes
- Archive `extract-library-crate` openspec change
- StorageHal: no concrete implementation in esp32 example (stubs return NOT_IMPLEMENTED)
- SSE `glass_size_warning` event: not yet emitted
- ESP32 bring-up: replace spin executor in `examples/esp32/main.rs` with `#[esp_hal::main]`
  + esp-hal/esp-hal-embassy/esp-wifi deps

---

### 2026-03-07: openspec-sync-specs glass-presence-check

**Changes made:**
- `openspec/specs/admin-config/spec.md`: Applied MODIFIED delta — updated `AdminConfig`
  requirement to include `glass_wait_timeout_secs` (default 60) and `drink_ready_timeout_secs`
  (default 300); added zero-means-indefinite and default-on-missing scenarios; replaced
  `Capabilities contains version` requirement with full 7-field list including
  `has_cancel_button` and `has_power_button`; updated `GET /config` scenario field list.
- `openspec/specs/async-hal-traits/spec.md`: Applied MODIFIED delta — version reference
  updated to 0.6.0; added ADDED requirement for `GlassWaitReason` and `RecoveryAction`
  HAL types.
- `openspec/specs/job-queue/spec.md`: Applied MODIFIED delta — `job-create` signature
  description updated (glass detection moved from server to HAL); `Queue flush` updated
  to include `WaitingForGlass` cancellation and `Active` wait semantics; added ADDED
  requirements for `JobState::Active` variant and cleaning-entry queue clear.
- `openspec/specs/sse-wiring/spec.md`: Applied MODIFIED delta — `Initial snapshot`
  updated to reference full data-carrying `RobotState` payload; added ADDED requirements
  for `state_change` full payload with example JSON and `glass_size_warning` event.
- `openspec/specs/glass-aware-state-machine/spec.md` (NEW): Created full spec from
  change delta — `RobotState` data-carrying enum table, `GlassWaitReason`, `RecoveryAction`,
  valid state transition table, glass type validation, cleaning terminal state,
  glass polling interval, optimistic stub.

**Status:** All living specs now reflect the implemented glass-presence-check state.
No code changes — spec sync only.

**Next steps:**
- Archive `glass-presence-check` and `implement-test-hal` openspec changes
- Implement glass-type size→volume lookup + part_ml scaling in dispense handler
- StorageHal: no concrete implementation (stubs return NOT_IMPLEMENTED error)
- SSE `glass_size_warning` event: not yet emitted (HAL stubs don't detect glass size)
- ESP32 bring-up: `#[esp_hal::main]` + esp-hal/esp-hal-embassy/esp-wifi deps

---

### 2026-03-07: glass-presence-check (v0.5.0 → v0.6.0 breaking)

**Commit:** 1cd94a7

**Changes made:**
- `src/hal/mod.rs`: Added `GlassWaitReason` enum (`NoGlass`, `TooSmall { detected_volume, required_volume }`)
  and `RecoveryAction` enum (`PutGlassBack`, `RemoveGlass`, `CallResetErrors`, `None`).
  Replaced flat `RobotState` enum with data-carrying tagged-union:
  `WaitingForGlass { job_id, reason, timeout_remaining_secs }`,
  `Working { job_id, progress_pct }`, `DrinkReady { job_id, timeout_remaining_secs }`,
  `Error { code, message, job_id, recoverable, recovery, timeout_remaining_secs }`;
  removed `Prepared`. Dropped `Deserialize` and `Eq` from `RobotState` (has `f32` fields).
  Replaced `JobState::Running` with `JobState::Active`.
  Added `glass_wait_timeout_secs: u32` (default 60) and `drink_ready_timeout_secs: u32` (default 300)
  to `AdminConfig` and `RobotConfig`. Added `has_cancel_button: bool` and `has_power_button: bool`
  to `Capabilities`.
- `src/hal/mock.rs`: Updated imports and test helpers (`test_robot_config`, `test_admin_config`,
  `MockConfigHal::update_active_config`) for new fields.
- `src/hal/tests.rs`: Rewrote `RobotState` serialisation tests for tagged-union shape;
  replaced `JobState::Running` with `JobState::Active`; added `GlassWaitReason` and `RecoveryAction`
  serialisation tests; added `AdminConfig` timeout default tests (missing fields → 60/300).
- `src/main.rs`: Updated `Capabilities` and `RobotConfig` default values with new fields.
- `src/esp32/sensors.rs`: Stub `glass_state()` now returns `present: true, confidence: 0.0`
  (optimistic — avoids blocking on glass detection in stub mode).
- `src/esp32/config.rs`: Added `has_cancel_button: false`, `has_power_button: false` to capabilities;
  added timeout fields to default config and `get_active_config()` mapping.
- `src/storage/ram.rs`: Added timeout defaults to `AdminConfig` initialisation.
- `src/server/handlers/status.rs`: Serialises `RobotState` as tagged-union via `serde_json::to_value`,
  merges `errors` key at top level. Added tests for tagged-union JSON shape.
- `src/server/handlers/cleaning.rs`: Added `StatusHal` generic; returns 409 if state is not
  `Idle` or `Provisioning`; updated all handler tests to pass `MockStatusHal`.
- `src/server/handlers/config.rs`: Changed `JobState::Running` → `JobState::Active` in queue-flush.
- `src/server/mod.rs`: Updated `handle_start` call site to pass `&self.hal.status`.
- `src/server/sse.rs`: `emit_state_event` serialises `RobotState` directly via `serde_json::to_value`
  instead of wrapping in `{"state": ...}`.
- `API.yaml`: Replaced `State` string enum with `RobotState` oneOf tagged-union schema
  (with `GlassWaitReason` and `RecoveryAction` sub-schemas); added `has_cancel_button` and
  `has_power_button` to `Capabilities`; added timeout fields to `AdminConfig` and `Config`;
  replaced `running` with `active` in `JobStatus.state`; updated `/status` response to
  reference `RobotState`; updated SSE examples.
- `openspec/changes/glass-presence-check/`: All change artifacts (proposal, design, 5 specs, tasks)
  created and all 29 tasks completed.

**Status:** `cargo test` — 113 tests, 0 failed. `cargo check --features esp32` — clean.

**Next steps:**
- Archive `glass-presence-check` and `implement-test-hal` openspec changes
- Implement glass-type size→volume lookup + part_ml scaling in dispense handler
- StorageHal: no concrete implementation (stubs return NOT_IMPLEMENTED error)
- SSE `glass_size_warning` event: not yet emitted (HAL doesn't detect glass size in stubs)
- ESP32 bring-up: `#[esp_hal::main]` + esp-hal/esp-hal-embassy/esp-wifi deps


### 2026-03-06: implement-test-hal (v0.5.0, no version bump — dev tooling only)

**Changes made:**
- `Cargo.toml`: Added `futures = "0.3"` dev-dep (for `block_on` in tests); confirmed
  `embedded-hal-mock = "0.7.2"` and `test-case = "3.2"` already in `[dev-dependencies]`
- `src/hal/mock.rs` (NEW): Test-only mock implementations for all 7 HAL traits
  (`MockControlHal`, `MockStatusHal`, `MockConfigHal`, `MockStorageHal`, `MockSensorHal`,
  `MockDispenseHal`, `MockCleaningHal`) plus `MockPasswordHasher` and `MockWrite`
  (embedded_io_async::Write backed by Vec<u8>). Each mock has stateful inspection fields,
  `fail_next`/`restore_fail`/`fail` for error injection, and a `done()` assertion.
  Helper functions: `test_error()`, `test_robot_config()`, `test_admin_config()`.
- `src/hal/mod.rs`: Added `#[cfg(test)] pub(crate) mod mock;`
- `src/hal/tests.rs`: Complete rewrite — all 7 trait test groups updated to current API
  (AdminConfig, backup/restore, JobCreated, async fn with block_on). Added `#[test_case]`
  parameterisation for RobotState, JobState, LevelReporting serialization. Added
  `crc32_hex_consistent` test.
- `src/main.rs`: Added `#[cfg(test)] mod server;` so handler files compile in test mode.
  Added `#[no_mangle] fn _embassy_time_now()` and `_embassy_time_schedule_wake()` no-op
  stubs so embassy-time links in test builds without a real time driver.
- `src/server/handlers/status.rs`: Added `#[cfg(test)]` block (2 tests)
- `src/server/handlers/control.rs`: Added `#[cfg(test)]` block (4 tests)
- `src/server/handlers/config.rs`: Added `#[cfg(test)]` block (4 tests)
- `src/server/handlers/sensors.rs`: Added `#[cfg(test)]` block (4 tests)
- `src/server/handlers/dispense.rs`: Added `#[cfg(test)]` block (4 tests)
- `src/server/handlers/cleaning.rs`: Added `#[cfg(test)]` block (4 tests)

**Status:** `cargo test` passes — 106 tests total (0 failed).
`cargo build` and `cargo check --features esp32` both clean.

**Key discovery:** embassy-time requires `_embassy_time_now` and `_embassy_time_schedule_wake`
symbols (Rust ABI, `#[no_mangle]`) when linked in test binaries. Provided as no-op stubs in
main.rs under `#[cfg(test)]`.

**Next steps:**
- Archive `implement-test-hal` change
- Implement glass-type size→volume lookup and part_ml scaling in dispense handler
- StorageHal: no concrete implementation (stubs return NOT_IMPLEMENTED error)
- SSE events: event model not designed yet
- ESP32 bring-up: `#[esp_hal::main]` + esp-hal/esp-hal-embassy/esp-wifi deps


### 2026-03-06: add-admin-password-auth (v0.4.0 → remains v0.4.0, backwards-compatible addition)

**Branch:** claude/add-admin-password-auth-3Mjmi

**Changes made:**
- `src/hal/mod.rs`: Added `PasswordHasher` trait (`hash`, `verify` methods); added
  `admin_password: String` field to `RobotConfig` with `#[serde(default)]`
- `src/server/http.rs`: Added `basic_auth_password()` — decodes `Authorization: Basic <b64>`,
  extracts password component
- `src/server/mod.rs`: Added `DEFAULT_ADMIN_PASSWORD`, `ADMIN_ROUTES` constant;
  added `Hasher: PasswordHasher` as 8th generic to `RobotHal`/`ApiServer`; split
  auth dispatch — admin routes use Basic Auth + `PasswordHasher::verify`, other
  routes keep Bearer token; updated `PATCH /config` call site to pass `&self.hal.hasher`
- `src/server/handlers/config.rs`: `handle_config_get` redacts `admin_password` (returns `""`);
  `handle_config_patch` accepts `H: PasswordHasher` — hashes non-empty `admin_password`,
  preserves existing hash when empty
- `src/main.rs`: Added `StubPasswordHasher` (constant-time XOR verify, `stub$<pw>` format);
  added `admin_password: String::new()` to stub config; wired into `RobotHal`
- `src/esp32/hasher.rs` (NEW): `Esp32PasswordHasher` using PBKDF2-HMAC-SHA256 via
  `pbkdf2` + `sha2` crates; format `pbkdf2$<iters>$<salt_hex>$<dk_hex>`;
  salt from `AtomicU32` counter (TODO: replace with `esp_hal::rng` for production)
- `src/esp32/mod.rs`: Added `mod hasher`; re-exported `Esp32PasswordHasher`;
  added `hasher: Esp32PasswordHasher` field to `Esp32Hal`; added `PasswordHasher for Esp32Hal`
- `src/esp32/config.rs`: Added `admin_password: String::new()` to default config
- `Cargo.toml`: Added `base64 = "0.22"` (alloc feature); `sha2 = "0.10"` and
  `pbkdf2 = "0.12"` (hmac feature) as optional deps gated on `esp32` feature
- `API.yaml`: Added `basicAuth` security scheme; added `admin_password` to Config schema
  (write-only, returns `""`); annotated all 9 admin routes with `security: [{basicAuth: []}]`;
  added description to `PATCH /config` about password re-hashing

**Status:** `cargo check` passes (default + `--features esp32`). `cargo fmt` clean.
